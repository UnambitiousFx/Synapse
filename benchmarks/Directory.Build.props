<Project>
    <Import Project="$([MSBuild]::GetPathOfFileAbove(&#39;build.props&#39;))"/>

    <PropertyGroup>
        <GeneratePackageOnBuild>false</GeneratePackageOnBuild>
        <IsPackable>false</IsPackable>
        <RootNamespace>$(Product).Benchmarks.$(MSBuildProjectName)</RootNamespace>
        <AssemblyName>$(Product).Benchmarks.$(MSBuildProjectName)</AssemblyName>
    </PropertyGroup>
</Project>
